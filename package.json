{
  "name": "malka-jobconnect",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "MALKA - Sistema de gestión de empleabilidad. Módulo de Vacantes.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "json-server --watch db.json --port 3000"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "json-server": "^0.17.4"
  }
}
