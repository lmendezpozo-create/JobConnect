{
  "name": "malka-brete",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "start /B json-server --watch ../../db.json --port 3000 && vite",
    "build": "vite build",
    "preview": "vite preview",
    "api": "json-server --watch ../../db.json --port 3000"
  },
  "devDependencies": {
    "vite": "^8.2.2",
    "json-server": "^0.17.4"
  }
}
