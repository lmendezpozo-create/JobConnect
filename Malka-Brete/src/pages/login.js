import { login,isAuthenticated,redirectByRole } from '../services/auth.js';if(isAuthenticated())redirectByRole();document.querySelector('#loginForm').onsubmit=async e=>{e.preventDefault();const d=new FormData(e.target);const error=document.querySelector('#error');error.textContent='';try{await login(d.get('username'),d.get('password'));redirectByRole()}catch(err){error.textContent=err.message==='Usuario o contraseña incorrectos'?err.message:'No se pudo conectar con el servidor.'}};
